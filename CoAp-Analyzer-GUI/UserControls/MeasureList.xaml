<UserControl x:Class="CoAP_Analyzer_GUI.UserControls.MeasureList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <ListView ItemsSource="{Binding Measures}">
            <ListView.View>
                <GridView>
                    <GridViewColumn DisplayMemberBinding="{Binding Time}" Width="Auto"/>
                    <GridViewColumn DisplayMemberBinding="{Binding IP}" Width="Auto"/>
                    <GridViewColumn DisplayMemberBinding="{Binding Value}" Width="Auto"/>
                    <GridViewColumn DisplayMemberBinding="{Binding Unit}" Width="Auto"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>

