<UserControl  x:Class="CoAP_Analyzer_GUI.UserControls.HostList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*" />
            <RowDefinition Height="25" />
            <RowDefinition Height="25" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="140" />
        </Grid.ColumnDefinitions>
        <ListView Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" x:Name="List" ItemsSource="{Binding Hosts}" SelectionChanged="List_SelectionChanged" MouseDoubleClick="List_MouseDoubleClick" BorderThickness="0">
            <ListView.View>
                <GridView AllowsColumnReorder="True">
                    <GridViewColumn Header="Host IP" DisplayMemberBinding="{Binding IP}" Width="200"/>
                    <GridViewColumn Header="Rate(ms)" DisplayMemberBinding="{Binding Rate}" Width="100"/>
                </GridView>
            </ListView.View>
        </ListView>
        <TextBlock x:Name="Selected" Grid.Row="1" Grid.Column="0" />
        <DockPanel Grid.Row="1" Grid.Column="1">
            <Button x:Name="Start_Stop" Content="Start" Width="70" Click="Start_Stop_Click" HorizontalAlignment="Left" />
            <Button x:Name="Remove" Content="Remove" Width="70" Click="Remove_Click" HorizontalAlignment="Left" />
        </DockPanel>
        <DockPanel Grid.Row="2" Grid.Column="0">
            <TextBox x:Name="NewHostBox" ToolTip="IPv6 Address" Width="215" HorizontalAlignment="Left" />
            <TextBox x:Name="NewRateBox" ToolTip="Rate(Seconds)" Width="35" HorizontalAlignment="Left" Margin="5,0,0,0" Padding="0" />
        </DockPanel>
        <DockPanel Grid.Row="2" Grid.Column="1">
            <Button x:Name="AddHost" Content="Add" Width="70" Click="AddHost_Click" HorizontalAlignment="Left" />
            <Button x:Name="ClearHost" Content="Clear" Width="70" Click="ClearHost_Click" HorizontalAlignment="Left" />
        </DockPanel>
    </Grid>
</UserControl>
