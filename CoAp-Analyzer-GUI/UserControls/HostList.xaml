<UserControl  x:Class="CoAP_Analyzer_GUI.UserControls.HostList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="400
              ">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*" />
            <RowDefinition Height="25" />
        </Grid.RowDefinitions>
        <ListView Grid.Row="0" x:Name="List" ItemsSource="{Binding Hosts}" MouseDoubleClick="List_MouseDoubleClick" BorderThickness="0">
            <ListView.View>
                <GridView AllowsColumnReorder="True">
                    <GridViewColumn Header="Host IP" DisplayMemberBinding="{Binding IP}" />
                    <GridViewColumn Header="Rate(ms)" DisplayMemberBinding="{Binding Rate}" />
                </GridView>
            </ListView.View>
        </ListView>
        <DockPanel Grid.Row="1" HorizontalAlignment="Center">
            <TextBox x:Name="NewHostBox" ToolTip="IPv6 Address" Width="150" />
            <TextBox x:Name="NewRateBox" ToolTip="Rate(Seconds)" Width="50" />
            <Button x:Name="AddHost" Content="Add Host" Width="70" Click="AddHost_Click" HorizontalAlignment="Left" />
            <Button x:Name="ClearHost" Content="Clear" Width="70" Margin="0,0,0,0" Click="ClearHost_Click" HorizontalAlignment="Left" />
        </DockPanel>
    </Grid>
</UserControl>
