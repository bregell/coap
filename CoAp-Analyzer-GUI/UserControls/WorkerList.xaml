<UserControl x:Class="CoAP_Analyzer_GUI.UserControls.WorkerList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:usr="clr-namespace:CoAP_Analyzer_GUI.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <ListView x:Name="workerlist" ItemsSource="{Binding Workers}" MouseDoubleClick="ListView_MouseDoubleClick">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Name"       DisplayMemberBinding="{Binding MethodName}" />
                    <GridViewColumn Header="IP"         DisplayMemberBinding="{Binding IP}" />
                    <GridViewColumn Header="Rate(ms)"   DisplayMemberBinding="{Binding Rate}" />
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>
